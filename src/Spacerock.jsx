/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { forwardRef, useRef } from "react";
import { useGLTF } from "@react-three/drei";

export default forwardRef((props, ref) => {
  const { nodes } = useGLTF("/solaris/models/spacerock-transformed.glb");
  return (
    <mesh ref={ref} {...props} dispose={null} geometry={nodes.spacerock.geometry}>
      {props.children}
    </mesh>
  );
});

const spacerockGeometry = () => {
  const { nodes } = useGLTF("/solaris/models/spacerock-transformed.glb");
  return nodes.spacerock.geometry;
};

useGLTF.preload("/solaris/models/spacerock-transformed.glb");

export { spacerockGeometry };
